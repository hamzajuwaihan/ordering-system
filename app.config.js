// app.config.js

const config = {
  ALERT_THRESHOLD: 0.5, //from here you can change the threshold value to any value you want to set as threshold for the email alert to be sent to the user
};

export default config;